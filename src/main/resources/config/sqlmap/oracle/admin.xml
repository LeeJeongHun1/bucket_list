<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.bucket.repository.mapper.AdminMapper">
<!-- 
<select id="AdminMapper" parameterType="SearchmemVO" resultType="SearchMemResult">
select u.NAME,u.REG_DATE,u.BIRTH,p.PACKAGE_PRICE,p.PACKAGE_NAME
  from (tb_user u inner join tb_payment p
    on u.USER_EMAIL = p.USER_EMAIL) inner join tb_package p 
    on u.USER_EMAIL = p.USER_EMAIL
 where name like '김%' 
    AND u.reg_date between TO_DATE(20180603,'YYYYMMDD') and  TO_DATE(20180605,'YYYYMMDD')
    AND substr(u.birth,1,4) between 1990 and 1994
</select>
 -->
</mapper>




