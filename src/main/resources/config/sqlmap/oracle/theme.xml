<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.bucket.repository.mapper.ThemeMapper">
	<select id="selectThemeList" parameterType="string">
		select img_path, theme_name, theme_price
		from tb_theme
		inner join tb_img_group
		on tb_img_group.theme_code = tb_theme.theme_code
		where theme_type = '관광';
	</select>
</mapper>




